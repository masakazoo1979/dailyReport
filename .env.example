# ===========================================
# 営業日報システム 環境変数設定
# ===========================================
# このファイルをコピーして .env として使用してください
# 本番環境では適切な値に置き換えてください

# -------------------------------------------
# Database Configuration
# -------------------------------------------
# PostgreSQL接続URL（必須）
# 形式: postgresql://[user]:[password]@[host]:[port]/[database]?schema=public
DATABASE_URL="postgresql://postgres:postgres@localhost:5432/daily_report?schema=public"

# -------------------------------------------
# Application Settings
# -------------------------------------------
# 環境: development | production | test
NODE_ENV="development"

# サーバーポート
PORT=3000

# -------------------------------------------
# NextAuth.js Configuration
# -------------------------------------------
# アプリケーションURL（CSRF保護に必要）
NEXTAUTH_URL="http://localhost:3000"

# セッション署名用シークレットキー（必須、32文字以上推奨）
# 生成コマンド: openssl rand -base64 32
# 重要: 本番環境では必ず一意の安全な値を使用してください
NEXTAUTH_SECRET="your-nextauth-secret-key-here-change-in-production"

# -------------------------------------------
# Optional: Production Settings
# -------------------------------------------
# GCP Cloud SQL接続例:
# DATABASE_URL="postgresql://user:password@/daily_report?host=/cloudsql/project:region:instance"

# Secret Manager使用時は環境変数を直接設定せず、
# Cloud Runのシークレット参照機能を使用してください
